# ConfigMap с переменными окружения для userservice (URL, среда исполнения и прочие настройки)
apiVersion: v1
kind: ConfigMap
metadata:
  name: userservice-config
data:
  ASPNETCORE_URLS: 'http://+:8000'
  ASPNETCORE_ENVIRONMENT: 'Production'
